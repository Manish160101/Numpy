# Numpy is python library to work with Arrays
# Array is part of numpy library for storing similar dataypes values in continuous memory
# It's Storage efficient and 50 times faster than Lists.
# It's type is ndarray

import numpy as np

arr = np.array(23)

print(type(arr))   #numpy.ndarray
print(arr.ndim)  # 0 - 0D array